name = "img"
main = "data.js"

compatibility_date = "2025-02-16"


[observability]
enabled = true

[site]
bucket = "./"  # Direktori tempat file WASM berada

[build]
command = "npx esbuild worker.js --bundle --outdir=dist"

[vars]
WASM_FILE = "djpeg-static.wasm"
